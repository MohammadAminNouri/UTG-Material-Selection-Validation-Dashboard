# Assumptions

- Property ranges are taken from the written report table and converted to midpoint values for app calculations.
- ML is decision-support only and uses the report's weighted matrix as the target.
- The bending relation uses the report simplification: sigma_bending = 12.1 E.
- Eco Audit sensitivity preserves Granta baseline non-use phases and scales use phase with power, daily time, and lifetime.
